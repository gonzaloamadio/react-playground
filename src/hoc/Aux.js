// import React from 'react';

// We only return props.children. Children is a special property that simply outputs
// whatever gets entered between opening and closing tags
// After React 16.2 this will be like React.Fragment
const aux = props => props.children;

export default aux;